{
  "name": "@coursemind/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "generate:prod": "prisma generate --schema=prisma/schema.prod.prisma",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy",
    "push:prod": "prisma db push --schema=prisma/schema.prod.prisma --skip-generate",
    "seed": "tsx prisma/seed.ts",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.8.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "prisma": "^6.8.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
